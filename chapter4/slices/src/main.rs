fn main() {
    //converting space to bytes
    // println!("{}", b' ');

    // let hello = "hello world!";
    // let h = &hello[..];
    // println!("{}", h);

    // can only print integers, not full arrays
    let a = [0, 1, 2, 3];
    println!("{}", a[1]);
}
